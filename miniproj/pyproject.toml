[build-system]
requires = [
    "meson-python","meson", "numpy"]
build-backend = "mesonpy"

[project]
name="miniproj"
version = "0.1.0"
description = "refactor"
requires-python = ">=3.9"
dependencies = ["numpy"]