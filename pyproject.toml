[build-system]
requires = [
    "meson-python","meson", "numpy"]
build-backend = "mesonpy"

[project]
name="tstrippy"
version = "0.1.0"
description = "tidal stripping simulations of star clusters"
requires-python = ">=3.9"